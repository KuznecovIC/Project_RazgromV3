# RAZGROM V3 🎧  
Музыкальная платформа в духе SoundCloud, но с упором на **красивый фронтенд**, удобную навигацию и **социальность**: почти все действия публичные (подписки, лайки, репосты и т.д.).

> Репозиторий: **KuznecovIC / Project_RazgromV3**  
> Статус: активная разработка (WIP)

---

## ✨ Что это умеет (по факту проекта)

### 🎵 Музыка
- Загрузка треков (аудио + обложка), метаданные (жанр, теги, описание, explicit и т.д.)
- Страница трека: проигрывание, лайки, репосты, комментарии, счетчики
- Waveform (данные + генерация waveform на бэке)
- История прослушиваний / “recently played”
- Фид треков + рекомендации

### 📚 Плейлисты
- Создание/редактирование/удаление плейлистов
- Добавление треков в плейлисты
- Лайки и репосты плейлистов
- Публичные страницы плейлистов

### 👤 Пользователи и социалка
- Регистрация/логин/логаут, сброс пароля
- Публичные профили + статистика (followers/following/tracks/reposts/playlists)
- Подписки: follow/unfollow, списки followers/following, подсказки “кого подписать”
- Аватар + header image + кастомный цвет GridScan
- “Now Playing” (показывает что слушает пользователь)

### 💬 Сообщения (чат)
- Диалоги (создание, список, скрыть/показать, удаление)
- Сообщения: текст + прикрепление трека
- Медиа в чате: голосовые, изображения, видео
- Реакции на сообщения
- Прочитано/не прочитано

### 🛡️ Модерация и безопасность
- Репорты (жалобы на пользователей)
- Баны: причина, кто выдал, дата/перманент
- Апелляции на бан (BanAppeal)
- Жёсткая блокировка забаненных на фронте (BanGuard / BannedScreen)

### 🤖 AI (опционально)
- AI-помощник модератору (анализ репортов/апелляций) через **Ollama**
- AI-рекомендации: “Made for you” (треки) и “Playlists for you”

---

## 🧱 Технологии (кратко)
- **Backend:** Django + Django REST Framework  
- **Frontend:** React (SPA, роутинг, контексты состояния)
- **AI (опционально):** Ollama (локально), модель по умолчанию: `qwen2.5:3b`

---

## 🗂️ Структура проекта (как ориентир)
> Названия папок могут отличаться, но по коду видно такую логику:

### Backend (Django)
- `models.py` — все модели (CustomUser, Track, Playlist, чат, репорты, апелляции и т.д.)
- `serializers.py` — сериализаторы API
- `views.py` — API endpoints (их много)
- `urls.py` — маршруты API
- `ai_ollama.py` — AI-интеграция (Ollama)

### Frontend (React)
- `src/App.js` — роутинг и глобальный проигрыватель/состояния
- `src/components/TrackPage.jsx` — страница трека
- `src/components/PlaylistPage.jsx` — страница плейлиста
- `src/components/ProfilePage.js` — профиль (очень большой файл)
- `src/components/Admin*` — админка (треки/плейлисты/репорты/юзеры)
- `src/components/Studio*` — “Artist Studio” (управление контентом, статистика)
- `src/context/*` — контексты (UserContext/SocialContext)
- `src/api/apiFetch.js` — запросы к API
- `src/components/BanGuard.jsx` — блокировка функционала забаненным

---

## 🚀 Быстрый старт (локально)

### 1) Backend (Django)
```bash
# 1) перейти в папку бэка
cd backend

# 2) виртуальное окружение
python -m venv .venv
# Windows: .venv\Scripts\activate
# Linux/Mac:
source .venv/bin/activate

# 3) зависимости
pip install -r requirements.txt

# 4) миграции + запуск
python manage.py migrate
python manage.py createsuperuser
python manage.py runserver